export interface Macros {
    calories: number;
    protein: number;
    carbs: number;
    fats: number;
}

export const emptyMacros: Macros = {
    calories: 0,
    protein: 0,
    carbs: 0,
    fats: 0,
}